// the root tsconfig file
// doesn't do anything, just references the other tsconfig files
{
  "files": [],
  "references": [
    {
      // config for the react app
      "path": "./tsconfig.app.json"
    },
    {
      "path": "./tsconfig.node.json"
    }
  ],
  "exclude": [
    "src/electron", // will make my own ts config
  ],
}